apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: orders-db
  annotations:
    platform.io/resource-type: database
    # Resource data lives in a sibling file (like backstage.io/techdocs-ref).
    # Resolved relative to this file → resources/orders-db-data.json.
    platform.io/resource-data: ./orders-db-data.json
spec:
  type: database
  owner: "group:default/payments"
